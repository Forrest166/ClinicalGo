# ClinicalGo
ClinicalGo is a fast and reliable clinical trial results extractor that combines regular expressions (Regex) with large language models (LLMs) to extract information from clinical trial abstracts in a high-throughput manner. LLMs are used for semantic understanding, while Regex is employed for proofreading, normalization, and denoising.
